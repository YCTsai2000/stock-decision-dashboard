# V6.2 deployment marker

V6.2 keeps the conservative event-reversal override, pauses normal shorts for live entry, unifies ranking logic, applies half-risk sizing to ordinary A setups and event-reversal trades, keeps A+ setups at full configured risk, and applies the session phase factor to leveraged-ETF execution sizing.
